server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/edwarparedes/config-service
          #search-paths: config-data - Solo en caso que este dentro de la carpeta config-data del repositorio git
  application:
    name: config-service